<h1>投稿一覧</h1>

<table>
  <thead>
    <tr>
      <th>投稿</th>
      <th>本文</th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
    <tr>
      <td><%= post.title %></td>
      <td><%= post.body %></td>
    </tr>
    <% end %>
  </tbody>
</table>